# -*- coding: utf-8 -*-
"""Per-tab controllers: read widget state, launch workers, render results.
Each controller wires (and unwires) only its own tab's signals — there is no
central wiring function."""
